{
  "name": "gtd-flow",
  "version": "0.8.2",
  "description": "Structured GTD plugin for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "deploy": "npm run build && { [ -f .deploy.env ] && . ./.deploy.env; VAULT=\"${OBSIDIAN_VAULT:?set OBSIDIAN_VAULT (env or .deploy.env) to your vault path}\"; mkdir -p \"$VAULT/.obsidian/plugins/gtd-flow\" && cp main.js manifest.json styles.css \"$VAULT/.obsidian/plugins/gtd-flow/\"; }",
    "test": "vitest run"
  },
  "devDependencies": {
    "@codemirror/state": "6.5.0",
    "@codemirror/view": "6.38.6",
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.0",
    "obsidian": "latest",
    "tslib": "^2.6.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
